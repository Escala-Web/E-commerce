import { FormularioStyle } from "./styles"

export const Formulario = ({children}) => {

    return (
        <>
            <FormularioStyle>
                {children}
            </FormularioStyle>

        </>
    )

}